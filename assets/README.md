# Screenshot slots

No product screenshots are included yet. Add only screenshots captured from the real Glowstone B-Roll Alpha workspace after the corresponding build is approved. Do not use mockups or invented UI.

Desired screenshots:

1. **First project setup** — shows a new project with a short script and the main setup controls.
2. **Analyzed semantic beats** — shows the beat rail and the relationship between a script segment and its visual query lanes.
3. **Provider browsing** — shows Pexels or Pixabay results and the separate NASA manual/search-first path.
4. **Preview and selection** — shows a selected asset, source information, and the review action.
5. **Export handoff** — shows the export controls or a redacted view of the resulting package contents.
6. **Settings and model setup** — shows provider configuration without API keys, private scripts, personal paths, or other sensitive data.

For each future image, record the Alpha version, capture purpose, and any redaction performed before adding it to a release.
